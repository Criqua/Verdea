---
import { Leaf } from 'lucide-react';
---
<section id="hero" class="relative bg-green-50 overflow-hidden">
  <!-- Hoja decorativa animada -->
  <Leaf className="hidden lg:block absolute top-0 right-0 w-64 h-64 text-green-200 opacity-40 animate-spin-slow pointer-events-none" aria-hidden="true"/>

  <div class="container mx-auto px-6 sm:px-8 lg:px-16 flex flex-col lg:flex-row justify-center items-center gap-12 py-20 sm:py-28 lg:py-32">
    <!-- Texto de la sección de hero -->
    <div class="w-full lg:w-1/2 space-y-5 text-center lg:text-left">
      <span class="inline-flex items-center gap-2 px-2 py-1 text-sm font-semibold rounded-full bg-gradient-to-r from-green-200 to-green-400 text-green-900 shadow-md ring-2 ring-green-300">
        <Leaf className="w-5 h-5 text-green-700" aria-hidden="true"/>
        100% Natural y Orgánico
      </span>

      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold text-green-900 leading-tight tracking-tight">
        Cuida tu piel con la <span class="text-green-600 bg-green-100 px-2 rounded">pureza de la naturaleza</span>
      </h1>

      <p class="text-gray-700 text-lg sm:text-xl max-w-xl mx-auto lg:mx-0">
        Descubre nuestra línea de higiene semi-artesanal, elaborada con ingredientes  
        naturales y empaques eco-friendly para un cuidado consciente y sostenible.
      </p>

      <div class="flex flex-col sm:flex-row justify-center mx-14 lg:justify-start gap-4 mt-8 sm:mx-0">
        <a href="#productos" class="inline-block px-8 py-3 bg-gradient-to-r from-green-500 to-green-600 text-white font-semibold rounded-lg shadow-lg hover:scale-105 hover:from-green-600 hover:to-green-700 focus:outline-none focus:ring-2 focus:ring-green-400 transition-transform duration-200">
          Ver Productos
        </a>
        <a href="#nosotros" class="inline-block px-8 py-3 border-2 border-green-500 text-green-700 font-semibold rounded-lg bg-white/80 backdrop-blur hover:bg-green-50 focus:outline-none focus:ring-2 focus:ring-green-400 transition">
          Conócenos
        </a>
      </div>
    </div>

    <div class="w-full lg:w-1/2 flex justify-center lg:justify-end">
      <div class="relative inline-block w-full max-w-md ml-6 lg:ml-0 overflow-visible">
        <!-- Borde negro de fuera (parte izquierda e inferior) -->
        <div class="absolute inset-0 border-3 border-black rounded-2xl -translate-x-6 translate-y-6 z-0"></div>

        <!-- Borde verde donde se contendrá la imagen de bg -->
        <div class="relative -inset-x-1 z-10 bg-green-500 rounded-2xl shadow-2xl overflow-hidden">

          <!-- Borde blanco dentro del bg -->
          <div class="absolute inset-0 m-3 border border-white rounded-2xl pointer-events-none z-20"></div>

          <img src="/images/background.svg" class="relative block w-full h-80 object-cover"/>
          
          <div class="absolute inset-0 my-9 mx-12 z-30 rounded-2xl overflow-hidden">
             <!-- Capa de degradado encima de la image para minimizar el choque de colores verde y correspondiente  -->
              <div class="absolute inset-0 rounded-2xl bg-gradient-to-tr from-green-100 to-transparent opacity-50 pointer-events-none"></div>
            <img src="/images/heroImage.webp" alt="Productos Verdea" class="w-full h-full object-cover ring-2 ring-white/60"/>
          </div>  
        </div>
      </div>
    </div>  
  </div>

  <style>
    .animate-spin-slow {
      animation: spin 18s linear infinite;
    }
    @keyframes spin {
      0% { transform: rotate(0deg);}
      100% { transform: rotate(360deg);}
    }
  </style>
</section>
